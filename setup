#!/bin/sh

#    ____    __
#   / __/__ / /___ _____
#  _\ \/ -_) __/ // / _ \
# /___/\__/\__/\_,_/ .__/
#                 /_/

# File:         setup
# Description:  setup script that SuperB
# Author:       NNB
#               └─ https://github.com/NNBnh
# URL:          https://github.com/NNBnh/dots/blob/master/setup


# Values
DOTS=$(cd -P -- "$(dirname -- "$0")" && pwd -P)

. "$DOTS/home/.config/env" # Get my environment variables

BAWKPACK_PATH="$DOTS/home/.local/bin/bawkpack"
BSYMLINK_PATH="$DOTS/home/.local/bin/bsymlink"

STEPS="${*:-install extra before symlink after}"


# Functions
install_packages() {
	"$BAWKPACK_PATH" "$DOTS/packageslist"
}

install_packages_extra() {
	# Python packages
	pip install --upgrade 'auto-editor' 'git+https://github.com/will8211/unimatrix.git'

	# ClipB
	git clone 'https://github.com/NNBnh/clipb.git' "$XDG_DATA_HOME/clipb" && ln -fs "$XDG_DATA_HOME/clipb/clipb" "$HOME/.local/bin/"

	# Terminal Explorer
	git clone 'https://github.com/NNBnh/terminal-explorer.git' "$XDG_DATA_HOME/terminal-explorer" && ln -fs "$XDG_DATA_HOME/terminal-explorer/te" "$HOME/.local/bin/"

	# BUI terminal
	git clone 'https://github.com/NNBnh/bui-terminal.git' "$XDG_DATA_HOME/bui-terminal" && ln -fs "$XDG_DATA_HOME/bui-terminal/bui-terminal" "$HOME/.local/bin/"

	# Bfetch
	git clone 'https://github.com/NNBnh/bfetch.git' "$XDG_DATA_HOME/bfetch" && ln -fs "$XDG_DATA_HOME/bfetch/bfetch" "$HOME/.local/bin/"

	# Fetchutils
	git clone 'https://github.com/lptstr/fetchutils.git' "$XDG_DATA_HOME/fetchutils"

	# Wallpapers
	git clone 'https://github.com/NNBnh/wallpaper-collections.git' "$XDG_DATA_HOME/wallpapers"

	# Textarts
	git clone 'https://github.com/NNBnh/textart-collections.git' "$XDG_DATA_HOME/textart"

	# Fonts
	aria2c --dir "$TMPDIR" $(curl --silent --location 'https://api.github.com/repos/NNBnh/bmono/releases/latest' | grep -e '/bmono-ttf.zip' | cut -d '"' -f '4') && 7z x -o"$TMPDIR/" "$TMPDIR/bmono-ttf.zip" && mv "$TMPDIR/ttf/"* "$XDG_DATA_HOME/fonts/"

	# Figlet fonts
	git clone 'https://github.com/xero/figlet-fonts.git' "$XDG_DATA_HOME/figlet/fonts"

	# Git alias
	curl -fsSL 'https://raw.githubusercontent.com/GitAlias/gitalias/master/gitalias.txt' --create-dirs --output "$XDG_DATA_HOME/git/gitalias.txt"

	# Charater picker
	mkdir -p "$XDG_DATA_HOME/charaters"
	curl -fsSL 'https://www.unicode.org/Public/emoji/13.1/emoji-test.txt' | sed -e 's/ E[0-9.]*/:/g' -ne 's/^.*fully-qualified *# //p' > "$XDG_DATA_HOME/charaters/emoji"
	curl -fsSL 'https://raw.githubusercontent.com/carloscuesta/gitmoji/master/src/data/gitmojis.json' | jq --raw-output '.gitmojis[] | [.emoji, .description] | join(": ")' > "$XDG_DATA_HOME/charaters/gitmoji"

	# Adblock
	curl -fsSL 'https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts' --create-dirs --output "$TMPDIR/hosts" && printf '%s' "127.0.1.1 $(hostname)" >> "$TMPDIR/hosts" && sudo mv "$TMPDIR/hosts" '/etc/hosts'
}

symlink_dotfiles() {
	     "$BSYMLINK_PATH" "$DOTS/home" "$HOME"
	sudo "$BSYMLINK_PATH" "$DOTS/root" '/'
}

before_symlink() {
	# Create directories (to only symlink files inside, not the directory)
	mkdir "$HOME/.config" "$HOME/.config/fcitx" "$HOME/.config/kak" "$HOME/.config/retroarch" \
	      "$HOME/.local" "$HOME/.local/bin" "$HOME/.local/share" \
	      "$HOME/.cache"

	# Remove junks (Spring cleaning your $HOME)
	mkdir "$HOME/test"
	mv "$HOME/Desktop" "$HOME/Documents" "$HOME/Downloads" "$HOME/Music" "$HOME/Pictures" "$HOME/Public" "$HOME/Templates" "$HOME/Videos" "$HOME/.ICEauthority" "$HOME/.bash_history" "$HOME/.bash_logout" "$HOME/.bash_profile" "$HOME/.bashrc" "$HOME/.inputrc" "$HOME/.xinitrc" "$HOME/.xsession" "$HOME/.xsession-errors" "$HOME/test/"
}

after_symlink() {
	# Change default shell
	chsh -s '/usr/bin/fish'

	# Change XDG directories
	for xdg_directory in DESKTOP DOCUMENTS DOWNLOAD MUSIC PICTURES PUBLICSHARE TEMPLATES VIDEOS; do
		eval "xdg-user-dirs-update --set '$xdg_directory' \"\$XDG_${xdg_directory}_DIR\""
	done

	# Enable firewall
	sudo ufw enable
	# Firefox CSS
	for profile in $HOME/.mozilla/firefox/*.default-release; do
		ln -fs "$DOTS/extra/firefox/chrome" "$profile/"
	done && echo 'Remember to enable the option toolkit.legacyUserProfileCustomizations.stylesheets in about:config'
}

# Start
for step in $STEPS; do
	case "$step" in
		'i'|'install') install_packages       ;;
		'e'|'extra')   install_packages_extra ;;
		'b'|'before')  before_symlink         ;;
		's'|'symlink') symlink_dotfiles       ;;
		'a'|'after')   after_symlink          ;;
		*)             exit 1                 ;;
	esac
done


exit 0
