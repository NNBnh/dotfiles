format = """
${custom.bui}\
$username\
$hostname\
$shlvl\
$kubernetes\
$directory\
$git_branch\
$git_commit\
$git_state\
$git_status\
$hg_branch\
$docker_context\
$package\
$cmake\
$dart\
$dotnet\
$elixir\
$elm\
$erlang\
$golang\
$helm\
$java\
$julia\
$nim\
$nodejs\
$ocaml\
$perl\
$php\
$purescript\
$python\
$ruby\
$rust\
$swift\
$terraform\
$zig\
$nix_shell\
$conda\
$memory_usage\
$aws\
$gcloud\
$openstack\
$env_var\
$crystal\
$lua\
$jobs\
$battery\
$time\
$cmd_duration\
$status\
$line_break\
$character
"""


[custom.bui]
when    = 'true'
format  = '$output'
command = 'bui-terminal'

[username]
format     = '[$user]($style)'
style_user = 'bright-yellow'
style_root = 'bright-white'

[hostname]
format = '[@](bold)[$hostname]($style)'
style  = ''

[directory]
format = ' [$path]($style) [$read_only]($read_only_style)'
style  = 'bold bright-white'
read_only  = ' '
read_only_style  = 'bright-black'
truncation_length = 0
truncate_to_repo  = false

[git_status]
format     = '($all_status$ahead_behind)'
conflicted = '[~$count](yellow) '
ahead      = '[⇡$count](green) '
behind     = '[⇣$count](red) '
diverged   = '[⇣$count](red) [⇡${count}](green) '
untracked  = '[?$count](purple) '
stashed    = '[$$count](cyan) '
modified   = '[!$count](yellow) '
staged     = '[+$count](green) '
renamed    = '[=$count](blue) '
deleted    = '[-$count](red) '

[cmd_duration]
format = '[took $duration]($style) '
style  = 'bright-black'

[status]
disabled = false
format   = '[$symbol$status]($style) '
style    = 'bright-black'
symbol   = 'E:'

[character]
format         = '$symbol '
success_symbol = '[ \$ ](fg:black bg:bright-yellow)[](bright-yellow)'
error_symbol   = '[ % ](fg:black bg:red)[](red)'
vicmd_symbol   = '[ V ](fg:black bg:bright-white)[](bright-white)'
